name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print greting
        run: echo "Hello word"
      - name: Print goodby
        run: echo "Done -bye!"
